# Build de producción (vite build). El API debe ser OTRO servicio Render (Spring Boot), no esta URL del front.
# Si tu API está en otro host, cámbialo aquí o define VITE_API_ORIGIN en Render → Environment (antes del build).
VITE_API_ORIGIN=https://despliegue-sportster-1.onrender.com
