# Perfil local: Postgres con docker-compose.yml (puerto 5432, contraseña sportster).
# Arranque: docker compose up -d
#          .\mvnw.cmd spring-boot:run -Dspring-boot.run.profiles=local
spring.datasource.password=sportster
