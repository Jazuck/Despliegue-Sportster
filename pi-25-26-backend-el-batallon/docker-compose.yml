services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: sportster
      POSTGRES_DB: sportster
    ports:
      - "5432:5432"
    volumes:
      - sportster-pgdata:/var/lib/postgresql/data

volumes:
  sportster-pgdata:
