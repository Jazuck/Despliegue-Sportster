# Web con raíz en backend: package.json puente delega en el frontend Vite.
services:
  - type: web
    name: despliegue-sportster
    runtime: node
    rootDir: pi-25-26-backend-el-batallon
    buildCommand: yarn
    startCommand: yarn start
